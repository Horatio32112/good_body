<!--
<h1>Times Fitness Records</h1>

<table>
  <thead>
    <tr>
      <th>Contents</th>
      <th>Duration (min)</th>
      <th>Distance (m)</th>
      <th>Time</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @times_records.each do |record| %>
      <tr>
        <td align="center"><%= record.contents %></td>
        <td align="center"><%= record.duration %></td>
        <td align="center"><%= record.distance %></td>
        <td align="center"><%= record.created_at.strftime("%Y-%m-%d %H:%M") %></td>
        <td>
          <%= button_to 'Update', edit_times_record_path(id: record.id), method: :get %>
        </td>
        <td>
          <%= button_to 'Delete', delete_times_record_path(record.id), method: :delete, onclick: "return confirmAction('Are you sure you want to delete this record?')" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

  <% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
  <% end %>

  <script>
function confirmAction(message) {
  var result = confirm(message);
  if (result) {
    // 如果使用者選擇確定，執行刪除操作
    return true;
  } else {
    // 如果使用者選擇取消，停止刪除操作
    return false;
  }
}
</script>

-->

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Times Fitness Records</title>
<style>
    body {
        font: 14px Arial, sans-serif;
        margin: 0px;
    }
    header {
        padding: 10px 20px;
        background: #ACB3B9;
        text-align: center; 
    }
    header h1 {
        font-size: 30px;
    }
    .container {
        width: 100%;
        background: #F2F2F2;
    }
    table {
        width: 100%;
        border-collapse: collapse;
    }
    th, td {
        padding: 8px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }
    th {
        background-color: #ACB3B9;
        color: white;
    }
    tbody tr:nth-child(even) {
        background-color: #f2f2f2;
    }
    .logout-link {
        position: absolute;
        top: 15px;
        right: 25px;
    }
    .notice {
        margin: 10px;
        background-color: #f2f2f2;
        padding: 10px;
        color: #333;
    }
    .homepage-button {
        margin: 20px;
        text-align: center;
        background-color: #F2F2F2;
        padding: 20px; 
    }
    .homepage-button a {
        display: inline-block;
        padding: 10px 20px;
        background-color: #ACB3B9;
        color: white;
        text-decoration: none;
    }

</style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Times Fitness Records</h1>
            <%= link_to 'Logout', logout_path, method: :get, class: 'logout-link', onclick: "return confirmAction('Are you sure you want to logout?')" %>
        </header>
        <table>
            <thead>
                <tr>
                    <th>Contents</th>
                    <th>Duration (min)</th>
                    <th>Distance (m)</th>
                    <th>Time</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% @times_records.each do |record| %>
                <tr>
                    <td align="center"><%= record.contents %></td>
                    <td align="center"><%= record.duration %></td>
                    <td align="center"><%= record.distance %></td>
                    <td align="center"><%= record.created_at.strftime("%Y-%m-%d %H:%M") %></td>
                    <td>
                        <%= button_to 'Update', edit_times_record_path(id: record.id), method: :get %>
                        <%= button_to 'Delete', delete_times_record_path(record.id), method: :delete, onclick: "return confirmAction('Are you sure you want to delete this record?')" %>
                    </td>
                </tr>
                <% end %>
            </tbody>
        </table>

        <% if flash[:notice] %>
        <p class="notice"><%= flash[:notice] %></p>
        <% end %>
        
        <div class="homepage-button">
            <%= link_to 'Back to Homepage', home_path %>
        </div>
        

        <script>
            function confirmAction(message) {
                var result = confirm(message);
                if (result) {
                    // 如果使用者選擇確定，執行刪除操作
                    return true;
                } else {
                    // 如果使用者選擇取消，停止刪除操作
                    return false;
                }
            }
        </script>
    </div>
</body>
</html>
