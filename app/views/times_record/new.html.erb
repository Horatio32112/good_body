
<!--
<h1>Create Times Fitness Record</h1>

<form action="<%= times_record_create_path %>" method="post">

  <%= hidden_field_tag 'record[user_id]', session[:user_id] %>


<label for="record_contents">Contents</label>
<input type="text" id="record_contents" name="record[contents]">

<label for="record_duration">Duration (min)</label>
<input type="number" id="record_duration" name="record[duration]">

<label for="record_distance">Distance (m)</label>
<input type="number" id="record_distance" name="record[distance]">


  <input type="submit" value="Create">
</form>

<% if flash[:error] %>
  <p><%= flash[:error] %></p>
<% end %>

-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Create Times Fitness Record</title>
    <style>
        body {
            font: 14px Arial, sans-serif;
            margin: 0px;
        }
        header {
            padding: 10px 20px;
            background: #ACB3B9;
            text-align: center;
            position: relative;
        }
        header h1 {
            font-size: 30px;
        }
        .logout-link {
            position: absolute;
            top: 15px;
            right: 25px;
        }
        .container {
            width: 100%;
            background: #F2F2F2;
        }
        form {
            margin: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        input[type="text"],
        input[type="number"] {
            width: 300px;
            padding: 8px;
            margin-bottom: 10px;
        }
        input[type="submit"] {
            display: block;
            background-color: #ACB3B9;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
        }
        .homepage-button {
            margin: 20px;
            text-align: center;
            background-color: #F2F2F2;
            padding: 20px;
        }
        .homepage-button a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #ACB3B9;
            color: white;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Create Times Fitness Record</h1>
            <%= link_to 'Logout', logout_path, method: :get, class: 'logout-link', onclick: "return confirmAction('Are you sure you want to logout?')" %>
        </header>
        <form action="<%= times_record_create_path %>" method="post">
            <%= hidden_field_tag 'record[user_id]', session[:user_id] %>

            <label for="record_contents">Contents</label>
            <input type="text" id="record_contents" name="record[contents]">

            <label for="record_duration">Duration (min)</label>
            <input type="number" id="record_duration" name="record[duration]">

            <label for="record_distance">Distance (m)</label>
            <input type="number" id="record_distance" name="record[distance]">

            <input type="submit" value="Create">
        </form>

        <% if flash[:error] %>
        <p><%= flash[:error] %></p>
        <% end %>

        <script>
            function confirmAction(message) {
                var result = confirm(message);
                if (result) {
                    // 如果使用者選擇確定，執行刪除操作
                    return true;
                } else {
                    // 如果使用者選擇取消，停止刪除操作
                    return false;
                }
            }
        </script>  

        <div class="homepage-button">
            <%= link_to 'Back to Homepage', homepage_path %>
        </div>
    </div>
</body>
</html>